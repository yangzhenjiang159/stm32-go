# AI × 人 协作调试指南（嵌入式 / 硬件）

> 源自本项目 **2026-09 点亮 3.5" TFT 液晶屏** 的实战总结（前后十几轮，最终靠"分步验证"一次打通）。
> 目标：**后续调试其它器件时，一次就拿到关键信息，不重复踩沟通的坑。**

---

## 0. 一句话总结

| 类别 | 做法 |
|---|---|
| 只能用肉眼看的 | 变成**结构化、可数、可比**的描述（**能拍照就拍照，图片放进工作区**） |
| 能用工具读的 | **全部交给 AI 自动读**（寄存器、内存、串口日志、编译日志） |
| 判断性结论 | 用**对照测试**（一次只改一个变量）替代"猜" |

---

## 1. 最有效的四件事（本次实战验证）

### 1.1 ⭐⭐⭐ 拍照 → 放进工作区（**最被忽略、收益最大**）

AI **可以直接读取工作区里的图片文件**。本次调试全程只有文字描述，是效率最大的损失点。

**做法**：
1. 手机拍屏幕/接线/丝印
2. 传到电脑，存到工作区，例如 `snapshots/lcd-20260915-1.jpg`
3. 对 AI 说：**"看 snapshots 里的 lcd-20260915-1.jpg"**

> 建议建一个 `snapshots/` 目录专门放这些图。

### 1.2 ⭐⭐⭐ 结构化现象描述（代替"乱了/不对/一半"）

**反面例子（本次真实出现，导致多轮返工）**：

| 模糊描述 | 问题 | 改成这样说 |
|---|---|---|
| "还是整块都是纯色啊，来回切" | 无法判断是背景色正常还是内容没画出来 | "整屏颜色每 4 秒在**黑↔蓝↔红**之间切换，**没有任何图形**出现" |
| "布局又乱了" | 完全不可定位 | "只有屏幕**右侧**有内容，是**一条竖直的黄色带**，从顶到底" |
| "只有部分横条" | 不知道几条、多宽 | "只看到 **3 条横条**（红/绿/蓝），第 4 条没有；横条**只占屏幕左边约 2/3 宽**" |
| "5/6 是白屏，一部分在动" | 比例不准 + "在动"含义不明 | "屏幕**约 1/3 区域**有内容，是**一个红色爱心在中间偏上**，其余是纯白" |

**正面例子（一说就定位）**：

- ✅ "从上到下 4 条横条：**红、绿、蓝、白**，四条都**铺满宽度**"
- ✅ "红色阶段**铺满整屏**；绿色阶段只在**左侧半屏**"
- ✅ "四角各有一个方块：**左上红、右上绿、左下蓝、右下黄**，大小都一样"

### 1.3 ⭐⭐⭐ 让 AI 出"自解释测试图案"

**这类图案最有信息量**（本次证明有效，已沉淀成模板）：

| 图案 | 用来判断 | 你只需要回答 |
|---|---|---|
| **四角定位**（四角各一个色块） | 坐标系/寻址是否正常、是否偏移 | "四个角都有吗？颜色位置对吗？" |
| **十字准星**（四角+中心） | 屏幕真实边界、原点位置 | "十字贴边吗？在角落吗？" |
| **等分色条**（横／竖各 4 条） | 屏幕真实宽高比 | "1/4、1/2 还是 2/3 铺满？" |
| **单色全屏** | 基础通路 | "哪种颜色能铺满？" |
| **单字/单块** | 内容绘制是否生效 | "中间出现字了吗？" |

**设计原则**：自解释（一个现象只对应一种原因）+ 可数（数量/比例）+ 有分隔（阶段间先用黑屏隔开）+ 静态优先（快速动画很难描述）。

### 1.4 ⭐⭐⭐ 分步验证（关键突破口，由你提出）

> 你当时说：*"你先用四个角定位，再在中间只输出一个字"* —— **一轮就定位成功**。

顺序永远是：**先证明"基础通路/坐标系"→ 再证明"内容绘制"→ 最后合成完整效果**。
不要一上来就烧完整功能程序，那样失败了完全无法定位。

---

## 2. 现象报告模板（直接复制）

```
【器件】3.5寸 TFT / 屏幕
【烧录的程序】lcd_love（圆润爱心版）
【期望现象】上电先红绿蓝全屏，然后黑底红字爱心+“老婆我爱你!”
【实际现象】
  - 颜色/内容：
  - 出现位置（上/下/左/右/中间，占屏幕多少比例）：
  - 有无变化（静止/循环/多久变一次）：
  - 与上一次相比，哪里变了：
【其它】背光亮不亮 / 板子有无异响发热 / 是否有照片（路径）
```

**几张"更好"的描述范例**：

- "屏幕**全白**，**没有任何**颜色或图形变化"
- "屏幕全黑，只有**中心**一个 100×100 的**白色方块**，位置在**正中间**"
- "出现**4 个竖条**：红、绿、蓝、黄，**每条占屏幕 1/4 宽**，都通到顶部和底部"

---

## 3. 硬件档案（填一次，长期复用）

**每次开工前，AI 应先读工作区根目录的 [`HARDWARE.md`](HARDWARE.md)** ——
里面记录开发板、已调试器件、接线表、工具链、已知坑。
调试新器件时，直接往里面追加一节即可。

---

## 4. AI 侧应遵循的调试协议

> 这部分是写给我（以及后续任何 AI）的约定。

1. **先读 `HARDWARE.md` 和器件资料，再动手改代码**；不要凭经验猜引脚/时序。
2. **优先客观测量**：能用工具读到的数据（寄存器、内存、串口打印、编译日志、`-r32` 读 RAM）绝不靠肉眼。
   - 主动建议用户接 **USB-TTL 串口**：固件里 `printf` 诊断信息，比 ST-Link 读内存方便得多。
3. **对照测试**：一次只改**一个**变量；同一图案分阶段轮流显示，让现象自解释（阶段之间用黑屏分隔，并明确告知每阶段停留时间）。
4. **测试图案必须"可数、可比"**：给出数量、比例、颜色、位置，避免"大概""有点"。
5. **分步验证**：基础通路（寻址/坐标）→ 内容绘制 → 完整功能。
6. **用户的直觉方案优先采纳**（例如"先四角定位再写字"这类建议往往是最高效的路径）。
7. **明确并记录"已知不可靠"的手段**，避免重复踩坑。例如本屏：GRAM 读回恒为 `0xFCFC`、读 ID 不稳定 → **不能设计依赖读回校验的诊断**。
8. **每轮结束告诉用户"这一轮在验证什么假设"**，让用户知道该看哪里。
9. **烧录顺序要串行**：不要连续烧两个固件（本次出现过一次"刚烧的固件被下一个覆盖、用户没看到"的返工）。
10. **结论必须回写文档**（`README.md` / `HARDWARE.md`），不要让经验只留在对话里。

---

## 5. 本次"沟通问题"造成的返工清单

| # | 沟通问题 | 造成的返工 | 改进 |
|---|---|---|---|
| 1 | 一直只用文字描述屏幕 | 十几轮烧录验证 | **拍照放进工作区** |
| 2 | "整块纯色来回切" | 无法区分"背景正常"还是"内容没画" | 描述里区分**背景色**与**图形** |
| 3 | "布局又乱了" | 无法定位 | 给出**位置 + 形状 + 颜色 + 比例** |
| 4 | 连续烧两个固件 | 用户看到的是后一个，结论被覆盖 | 一次只烧一个，等反馈 |
| 5 | 用"全屏纯色"判断窗口寻址 | 误以为正常（其实内容全被挤成一团） | 必须用**分离的小图形**验证 |

---

## 6. 待调试器件清单（可随时追加）

| 器件 | 状态 | 接口 | 备注 |
|---|---|---|---|
| 3.5" TFT（ILI9488 风格） | ✅ 已完成 | FSMC 16 位并口 | 见 `lcd_love/README.md` |
| （下一个器件…） | ⬜ 待开始 | | 请先在 `HARDWARE.md` 补一节：**型号/资料位置/接线表/供电/期望现象** |
