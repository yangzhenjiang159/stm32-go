# 前置工作核查表（本工作区专用）

> **起因**：做完 `lcd_love` 后发现"前置工作没做"。
> 本表把 `docs/` 下 6 篇方法论，折算成**本工作区可直接打勾的清单**，并如实标注现状。
>
> 定位：本文是**执行手册**，不是方法论。方法论见 `AI用于嵌入式的最佳实践.md` / `Win11-AI嵌入式工程落地方案.md`。

---

## 0. 前置工作链条（6 篇文档的共识）

```
阶段 0  文档核查（硬件文档一致性核查方案.md）
           ↓ 产出 HARDWARE-TRUTH.md（带出处 + 置信度）
           ↓ 只回填 HIGH / MEDIUM
        AGENTS.md §1（AI 的硬件事实来源）        ← AI 上下文层
           ↓
阶段 1  环境（Win11 方案 §2） 工具链 / 烧录器 / 环境自检
阶段 2  护栏（Win11 方案 §4） AGENTS.md + RULE.mdc + 格式/静态分析配置
阶段 3  验证闭环（Win11 方案 §5） 单测 → 静态分析 → 构建 → size → 上板
阶段 4  能力接入（Win11 方案 §6） 脚本 / MCP（含**烧录闸门**）
阶段 5  门禁（Win11 方案 §7） CI + 提示词模板
```

> **顺序不可颠倒**：`AGENTS.md` 填错 → 后面所有护栏都在**错误前提**上运行（阶段 0 文档 §0 的原话）。

---

## 1. 现状对照表

| 阶段 | 文档要求（出处） | 本工作区现状 | 状态 |
|---|---|---|---|
| **0 文档核查** | 7 步 → `HARDWARE-TRUTH.md` + 冲突登记（阶段 0 全文） | 今日补齐：`docs/hardware-truth/`（事实表 + 4 条冲突登记）；**6 项 `UNKNOWN` 未测**（电源/负载电容/BOOT0/触摸 IC/PCB 日期） | 🟡 已补核心 |
| **1 环境** | Win11 §2（CubeCLT / CMake / Ninja / 自检脚本） | `gcc-arm/`（GCC 10.3）+ `ST-LINK_CLI.exe` 均就位并**实测可用**；工具链未加 PATH（脚本用绝对路径）；无 CMake/CubeMX（本项目**不需要**） | 🟢 够用 |
| **2 护栏** | §4.1 `AGENTS.md`、§4.2 `RULE.mdc`、§4.4/4.5/4.6 格式与静态分析、§4.7 `.gitignore/.gitattributes` | ✅ 今日新建 `AGENTS.md`、`.codebuddy/rules/embedded-firmware/RULE.mdc`；❌ 无 `.clang-format` / `.clang-tidy` / `cppcheck` / `.gitignore` | 🟡 核心已补 |
| **3 验证闭环** | §5 `app/`(纯逻辑) + `tests/` + CMake + `ctest` + `analyze.ps1` + `verify.ps1` | 单测 ❌；`app/` 分层 ❌；`size-report` ⚠️（`build_gcc.ps1` 末尾已打印 size，等价）；静态分析 ❌ | ❌ **最缺的一环** |
| **4 能力接入** | §6 MCP（构建/尺寸/串口/烧录闸门）+ `compile_commands.json` | MCP ❌。当前"AI 只给命令、人工烧录"靠**纪律**而非机制（效果等同闸门，但无令牌约束） | ⚠️ 靠纪律 |
| **5 门禁** | §7 `ci.yml` + `prompts/firmware-tasks.md` | 无 CI；**连 git 仓库都没有**（`git status` 报 `not a git repository`）→ 无法小步提交 / 回滚 | ❌ 缺 |
| **结论回写** | Win11 §4.3、DEBUG_WORKFLOW §4 第 10 条 | `HARDWARE.md` / `DEBUG_WORKFLOW.md` / `PROJECT_STATE.md` / `lcd_love/README.md` 均已建，结论已回写 | 🟢 做得好 |

---

## 2. 这次为什么花了十几轮（事故 ↔ 文档条款对照）

> 这张表就是"前置工作没做"的代价账单。

| 本次真实事故 | 对应文档条款 | 前置做到可省掉什么 |
|---|---|---|
| 照抄资料例程 → **纯白屏**，反复十来轮 | 阶段 0 §4「系列手册 ≠ 型号手册」、§6.2 第 4 类（封装错配） | 一次三方对照即可判定"资料是 37 pin ILI9486 模块，**根本不属于本板**" |
| 用官方 `Template.hex` 判断硬件好坏 | 阶段 0 §7.1（实物实测优先级最高） | 早知 `LCD_RST` 是 PG15 不是 PC5，就不会拿它当基准 |
| 白屏先怀疑初始化序列，后才发现是 **FSMC 时序** | 阶段 0 §6.2 第 9 类（时钟/时序不符） | 阶段 0 表 2「时钟树」先记下 HCLK=72MHz，就能立刻怀疑时序 |
| "整块纯色来回切" / "布局又乱了" 等描述导致返工 | DEBUG_WORKFLOW §1.2（结构化现象）、§5 返工清单第 2/3 条 | 一开始就按"位置 + 形状 + 颜色 + 比例"描述，或直接拍照 |
| 用**全屏纯色**判断窗口寻址 → 误判正常 | Win11 §1.3（自解释测试图案）、DEBUG_WORKFLOW §5 第 5 条 | 前置定下"用四角方块验证寻址"，可直接定位 |
| 连续烧两个固件，用户看到的其实是后一个 | DEBUG_WORKFLOW §4 第 9 条 | 一次只烧一个，等反馈 |
| 想靠"读回 GRAM"做自动校验，白费功夫 | DEBUG_WORKFLOW §4 第 7 条（记录已知不可靠手段） | 若前置已记录，直接跳过 |

**结论**：这 7 条全部落在"**阶段 0 + 协作协议**"两类前置工作上 —— 正是你察觉缺的那两块。

---

## 3. 下次开新项目 / 新器件的最小动作清单

> 来自 Win11 方案 §13「最小可落地子集」+ 阶段 0 的 Gate P0，按本工作区形态裁剪。

**开工前（必须）**

- [ ] 新器件按 `HARDWARE.md` §3 模板补一节：型号 / 资料位置 / 接线表 / 供电 / **期望现象** / **已知可工作的参考 hex**
- [ ] 若资料与实物来源不同：执行阶段 0 的 Step 1（身份确认）+ Step 4（三方对照）→ 冲突写入 `docs/hardware-truth/07-conflicts.md`
- [ ] `AGENTS.md` §1 加入新器件的引脚占用（**禁止凭记忆猜引脚**）
- [ ] 有厂商 hex 先单独验证硬件通路 —— **且先确认那个 hex 属于本板**

**开工中（必须）**

- [ ] 现象先拍照放 `snapshots/`，再用结构化模板描述（`DEBUG_WORKFLOW.md` §2）
- [ ] 一次只改一个变量；每轮说明"这轮在验证什么假设"
- [ ] 诊断图案要可数 / 可比 / 自解释；**不用纯色全屏**验证寻址
- [ ] 烧录**一次只烧一个**

**收工（必须）**

- [ ] 编译 + 报 `arm-none-eabi-size` 增量（Flash 512 K / RAM 64 K）
- [ ] 结论回写 `HARDWARE.md` + `PROJECT_STATE.md` + 器件 README
- [ ] 新的坑补进 `AGENTS.md` §8 易错点速查

---

## 4. 本工作区剩余待办（按性价比排序）

| # | 事项 | 收益 | 成本 | 说明 |
|---|---|---|---|---|
| 1 | **`git init` + `.gitignore` + `.gitattributes`** | 换回滚能力（Win11 方案 §6 要求"小步提交可回滚"），出问题可 diff / 回退 | 约 10 分钟 | 需你确认后执行；忽略 `Obj/*.o`、`*.elf`、`*.map`、`*.a`，保留 `.hex` |
| 2 | **把纯逻辑抽出来做 PC 单测** | 把"改完烧板子才知道对不对"变成"秒级可验证"（Win11 §13 的第 3 件事） | 半天 | 可 PC 测的：心形轮廓插值、文字排版居中、配色表索引、扫描线填充。**裁剪版**：不用 CMake，直接用 PC 上的 gcc 编译「纯逻辑.c + test_main.c」跑断言即可 |
| 3 | `cppcheck` 静态分析 | 提前抓越界 / 未初始化 | 30 分钟 | 当前是单文件小工程，收益一般，可延后 |
| 4 | MCP 能力接入 | 把"给命令 + 人工烧录"变成**机制化闸门**（令牌 + 预览） | 1 小时 | 本工作区只需 `embedded_server.py` 的裁剪版：`build / size / flash(预览) / reset` 四个工具 |
| 5 | 补完阶段 0 的 `UNKNOWN` 项（§6 待验证 #V-001~#V-004） | 接传感器 / 串口 / 触摸前必须知道 | 万用表 10 分钟 | 见 `docs/hardware-truth/HARDWARE-TRUTH.md` §6 |

---

## 5. 一句话总结

`lcd_love` 的问题**不在代码，而在"开工前没有把事实固定下来"**。
现在 `AGENTS.md`（事实 + 约束 + 闸门）+ `docs/hardware-truth/`（带出处的核查结论 + 冲突登记）已经补齐，
**阶段 3（可秒级验证的验证闭环）是下一个真正的短板**，其余都是可选优化。
