---
description: STM32 裸机固件编码硬约束与操作审批闸门（禁动态内存/递归/VLA/热路径浮点，烧录必须人工确认，硬件参数必须引用 AGENTS.md）
alwaysApply: true
enabled: true
---

# 固件硬约束（STM32F103ZET6 · 3.5" TFT）

适用本仓库任何 C 代码改动。生成或修改代码前先默认套用以下规则，不要等用户提醒。
配套事实来源：`AGENTS.md`（硬件事实）· `docs/hardware-truth/HARDWARE-TRUTH.md`（带出处的事实表）。

## 禁止清单

- 禁止 `malloc` / `calloc` / `realloc` / `free` 等任何动态分配（本工程无堆）。
- 禁止递归，包括间接递归与"回调套回调"式的隐式递归。
- 禁止 VLA（`int buf[n]`）、`alloca`、可变长栈分配。
- 禁止在逐像素 / 逐行等**高频热路径**内新增 `float` / `double`（Cortex-M3 **无 FPU**，全部走软件浮点）。
- 禁止修改 `lcd_love/GCC/` 下的启动文件、`system_stm32f10x.c`、链接脚本，或编译 `core_cm3.c`。
- 禁止硬件寄存器裸魔数：必须用 CMSIS 宏或具名 `#define`（屏 IC 命令码如 `0xF7`/`0x3A` 需保留字面值并注释含义）。
- 禁止改动以下**已实测定型**的屏参数，除非明确说明理由并获得确认：
  - FSMC 时序 `ADDSET=15` / `DATAST=255`
  - 初始化序列含 `0xF7`（ILI9488 风格）
  - `0x3A = 0x55`（16bit/像素）
  - `0x36 = 0x08`（320×480 竖屏）
  - 复位脚 `PG15`、`CS=PG12(NE4)`、`RS=PG0(A10)`
  - `0x2A/0x2B` 各写 **4 次 8 位值**（不得改成 2 次 16 位）
- 禁止手改生成文件 `User/love_font.h`、`User/heart_table.h`（改 `tools/*.py` 后重新生成）。
- 禁止依赖"读 GRAM 校验"做诊断（本屏读回恒为 `0xFCFC`）。
- 禁止为了"让它编译过"而放宽时序、注释掉检查或绕过本文件的约束。

## 强制要求

- 所有缓冲区 / 点阵表 / 调色板容量**静态确定**，注释写明最坏情况字节数。
- 设计**诊断/测试图案**时：必须可数、可比、自解释（数量/比例/颜色/位置），阶段间用黑屏分隔；**不要用纯色全屏**验证窗口寻址。
- 新增或修改动画逻辑时，保持"分步验证"可执行：基础通路（四角定位）→ 内容绘制（单个汉字）→ 完整效果。
- 每次改动必须能通过 `powershell -File lcd_love\build_gcc.ps1` 编译，并报告 `arm-none-eabi-size` 输出。

## 操作审批闸门（最重要）

- **烧录、擦除、写选项字节、改 BOOT 配置：AI 一律不执行**。只输出命令与目标地址，由人工执行。
- **一次只输出/执行一个固件的烧录指令**，等用户反馈现象后再进行下一步。
- 改 `Interface/`、`Hardware/`、`GCC/` 前，先说明影响与理由。
- 不主动执行 `git commit` / `git push`。
- 可自由执行：读文件、搜索、编译（`build_gcc.ps1`）。

## 回答格式要求

- 涉及引脚、寄存器、时序、初始化序列的结论，**必须**引用 `AGENTS.md` §1 或 `docs/hardware-truth/HARDWARE-TRUTH.md`，不得凭经验猜。
- 涉及硬件行为的结论，若未上板实测，必须显式标注"**未经上板验证**"。
- 完成后按 `AGENTS.md` §7 DoD 逐条自查，给出实际编译输出摘要，不要用"应该没问题"代替证据。
- 需要用户观察现象时，给出**结构化现象报告模板**（见 `DEBUG_WORKFLOW.md` §2），并提醒可把照片放进 `snapshots/`。
